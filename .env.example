# Postgres connection
# Option 1: one URL
# DATABASE_URL=postgres://user:pass@localhost:5432/innsync

# Option 2: individual vars
# PGHOST=localhost
# PGPORT=5432
# PGUSER=postgres
# PGPASSWORD=postgres
# PGDATABASE=innsync

# SSL (for cloud dbs)
# PGSSL=false

# Server port
PORT=8080
